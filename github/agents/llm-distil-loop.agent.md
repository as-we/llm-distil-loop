---
description: Run 4-step LLM → ML distillation loop: generate labels → prepare data → train → validate accuracy
---

Execute `/llm-distil-loop` command from the llm-distil-loop extension.

See `speckit/commands/llm-distil-loop.md` for the full workflow.
